#pragma once
void check(int  firstNumber , float secondNumber);
float mult(float firstNumber, float secondNumber);
int time(int seconds, int minutes, int hours);
int countChar(char phrase, char character);
